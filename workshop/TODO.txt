* I shouldn't be able to input 0000.23 -> during lost focus
* Create dialogs for Load & Save
* On Load, take care of null, empty values

spy: disambiguate lambda functions!

Features:
* (ttest) if a cell is empty, when you click save or click the "t-test" button, that cell is filled with zero.

+ I use the column with the most unique values as the dependent variable

* Double click in the labels changes the values.

* Hovering the cursor over the labels changes the cursor to the pointer of the "Entry" widget.

* x-axis should have for this data set only 3 values for "supp"

* Create a separate section in the GUI for the Output.