* Create dialog for Save
* Remove regex check but demand dependent variable column to be all floats.

Features:
* Double click in the labels changes the values.

* Hovering the cursor over the labels changes the cursor to the pointer of the "Entry" widget.

* Create a separate section in the GUI for the Output.
	* Create a separete window in the GUI for the Output.